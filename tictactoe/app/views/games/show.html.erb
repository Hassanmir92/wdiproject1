<p id="notice"><%= notice %></p>


<p>
  <strong><%= [@game.user1.username, @game.user2.username].join( " VS ") %></strong>
</p>

<%= result_for_game(@game) %>

<%= current_turn_for_game(@game) %>

<%= render 'games/table' %>

<%= link_to 'Back', games_path %>

